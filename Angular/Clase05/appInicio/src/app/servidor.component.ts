import { Component }  from "@angular/core"

@Component({
    selector: "app-servidor",
    templateUrl: "./servidor.component.html",
    styleUrls: ["./servidor.component.css"]
    /*
     styles: [
        `h1 {color: red}`
     ]
    */
    /*
        selector: ".app-servidor" busca una etiqueta con la clase app-servidor

        selector: "[app-servidor]" busca una etiqueta con el atributo app-servidor
    */ 
    // template: "<h1>Servidor Principal</h1>"
})
export class Servidor{

}