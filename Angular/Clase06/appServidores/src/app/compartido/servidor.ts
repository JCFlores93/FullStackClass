export class Servidor {
    nombre: string
    descripcion: string
    estado: string
    area: string

    constructor(nombre: string, descripcion: string, estado: string, area: string){
        this.nombre = nombre
        this.descripcion = descripcion
        this.estado = estado
        this.area = area
    }
}
